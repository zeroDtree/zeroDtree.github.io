---
title: 环论(幂运算)
---

- [前置](#前置)
- [幺环上的幂运算](#幺环上的幂运算)
- [交换幺环上的幂运算](#交换幺环上的幂运算)
- [无零因子幺环上的幂运算](#无零因子幺环上的幂运算)
- [有序幺环上的幂运算](#有序幺环上的幂运算)
- [域上的幂运算](#域上的幂运算)
- [有序域上的幂运算](#有序域上的幂运算)

## 前置

- [[抽象代数/群论(幂运算)]]

![[抽象代数/群论(幂运算)]]

## 幺环上的幂运算

因为幺环关于乘法构成一个幺半群，因此可以定义幺半群的上**自然数幂**

## 交换幺环上的幂运算

因为交换幺环关于乘法构成一个交换幺半群，因此也可以定义**自然数幂**

## 无零因子幺环上的幂运算

因为无零因子幺环是一种幺环，因此也可以定义**自然数幂**

1. 若$n > 0$，则$x^n = 0$当且仅当$x = 0$

## 有序幺环上的幂运算

因为有序幺环是一种幺环，因此也可以定义**自然数幂**

注意有序环一定为无零因子环。

1. 若$n>0 \wedge x \gt y \gt 0$，则$x^n \gt y^n \gt 0$。
2. $|x^n| = |x|^n$

## 域上的幂运算

因为域上的非零元素关于乘法构成一个交换群，因此也可以定义**整数幂**

## 有序域上的幂运算

因为有序域上是域，因此也可以定义**整数幂**

1. If $x \gt y > 0$, then $x^n \gt y^n > 0$ if $n$ is positive, and $0 < x^n \lt y^n$ if $n$ is negative.
2. If $x, y > 0$, $n \not= 0$, and $x^n = y^n$, then $x = y$.
3. $|x^{-1}| = |x|^{-1}$
4. $|x^n| = |x|^n$
